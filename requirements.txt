# 媒体库浏览器 - Python依赖列表
# 安装命令: pip install -r requirements.txt

# ============== 核心依赖（必需） ==============
# 图像处理库 - 用于缩略图显示和图片查看
Pillow>=9.0.0

# ============== 演员管理功能（推荐） ==============
# 中文拼音转换 - 用于演员按拼音排序
pypinyin>=0.47.0

# 日文罗马音转换 - 用于日文演员名排序（可选）
pykakasi>=2.2.0

# ============== 爬虫功能（可选） ==============
# HTML解析库 - 爬虫模块必需
beautifulsoup4>=4.11.0

# 浏览器渲染 - 用于需要JS渲染的网页爬取（二选一）
# playwright>=1.30.0
# selenium>=4.8.0

# ============== 开发调试（可选） ==============
# 代码语法检查
# pyflakes>=3.0.0
